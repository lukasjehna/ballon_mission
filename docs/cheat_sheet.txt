pmc.connect() #Nach dem Start und nach dem Wechsel von GUI/Labview auf Python immer wieder den pmc.connect() Befehl ausführen.

pmc.setup_pmcc(pmc_backend.load('config/allregs.bin'), bandwidth='2GHz',int_time_ms=500)  #Standard Einstellungen laden

pmc.readReg(0)	#Check der Verbindung. Sollte 6 ausgeben

plothist(pmc.readADC()) #ADC Auslastung testen. Sollt Gauß Verteilung von 0-60 ergeben.

save(pmc.readAll(),'test.bin') #register auslesen und in Datei Speichern, wenn man gute Einstellungen gefunden hat.

d,t=pmc.meas_spectra(5) #Hier 5 Spektren
plot_spectrum(d,bandwidth = 4)
t	#absolute Zeit Messdaten
dt(t)	#relative Messzeit (Die benutzt man eher)
